{
  "note": "Adversarial inputs for the sqlite3_complete differential corpus. The rest of the corpus is every cumulative line-prefix of the shipped migration files, rebuilt from those files rather than committed. Each entry below targets one branch of the state machine that a hand-written splitter gets wrong: trigger bodies, comment tails, unterminated quotes and brackets, and the near-keywords the token classifier must NOT match.",
  "adversarial": [
    "",
    " ",
    "\n",
    ";",
    "  ;  ",
    "SELECT 1",
    "SELECT 1;",
    "SELECT 1; -- tail",
    "SELECT 1; /* open",
    "SELECT 'a;b';",
    "SELECT 'unterminated;",
    "SELECT \"x\";",
    "SELECT `x`;",
    "SELECT [x];",
    "SELECT [x;",
    "-- only a comment",
    "-- only a comment\n",
    "/* block */ SELECT 1;",
    "/* block */",
    "CREATE TRIGGER t BEFORE DELETE ON x BEGIN SELECT 1; END;",
    "CREATE TRIGGER t BEFORE DELETE ON x BEGIN SELECT 1;",
    "CREATE TEMP TRIGGER t BEFORE DELETE ON x BEGIN SELECT 1; END;",
    "CREATE TEMPORARY TRIGGER t BEFORE DELETE ON x BEGIN SELECT 1; END;",
    "EXPLAIN SELECT 1;",
    "creates;",
    "ends;",
    "CREATE TABLE t (a);",
    "CREATE TRIGGER t BEGIN SELECT 1; END",
    "SELECT 1;\n\n",
    "SELECT 1 /* c */ ;"
  ]
}
