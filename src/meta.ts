/** Package identity, used by CLI banners and by the smoke tests. */
export const PACKAGE_NAME = "@suisya-systems/continuo";

/**
 * Bootstrap placeholder. The package is `private: true` until publication is a
 * deliberate decision, so no release process reads this yet.
 */
export const PACKAGE_VERSION = "0.0.0";
